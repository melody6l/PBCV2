# 🧾 PBC 文件核对工具 — 功能全景介绍

PBC 文件核对工具是一款专为**审计场景**设计的桌面工具，帮助审计团队高效完成"被审计单位提供资料（PBC）"与"客户实际提交文件"之间的核对工作。

---

## 作品背景及内容简述

### 一、解决的实际业务问题

在审计实务中，每个审计项目都涉及大量 **PBC（Prepared by Client）资料**的收集与核对工作。典型场景如下：

1. **清单编制繁琐**：审计团队需要根据审计科目，手工编制 PBC 需求清单（通常包含几十到上百项资料需求），涉及多家被审计单位时，每家公司都需要独立的获取状态跟踪，Excel 手工维护极易出错。

2. **文件核对低效**：客户提交的资料通常散落在多层嵌套文件夹中，文件名写法五花八门——同一份"科目余额表"，客户可能命名为 `科目余额表.xlsx`、`TB_2024.xlsx`、`Trial Balance_CN02.xlsx` 等。审计人员需要逐个打开文件夹，肉眼比对清单与实际文件，**一个中型项目的资料核对往往耗费半天到一天时间**。

3. **公司归属混乱**：多家被审计单位的资料混放在同一目录下时，仅靠文件名有时难以判断属于哪家公司（如多公司共用一份合并报表），手工标记归属极易张冠李戴。

4. **进度难以追溯**：传统方式下，核对进度只存在于审计人员的脑子里或零散的 Excel 批注中。中途换人接手，需要重新熟悉全部资料状态，效率损失严重。

5. **归档整理耗时**：核对完成后，需要将散乱的文件按科目、公司重新整理归档到底稿目录，手工复制粘贴、创建文件夹、统一命名，又是一到两小时的机械劳动。

### 二、使用工具前后的变化

| 环节 | 使用前（传统 Excel 方式） | 使用后（PBC 核对工具） |
|------|--------------------------|------------------------|
| **清单编制** | 手工在 Excel 中罗列科目→PBC→需求资料，多公司时复制多份 sheet，耗时 30-60 分钟 | 勾选科目一键生成，自动生成多公司交叉矩阵，耗时 2 分钟 |
| **文件核对** | 逐个打开文件夹，肉眼比对文件名，人工标记状态，耗时 2-4 小时 | 扫描文件夹后自动匹配，**秒级完成**，匹配准确率通常 80% 以上 |
| **未匹配处理** | 逐条排查，凭经验猜测文件对应关系 | AI 辅助语义匹配兜底，待认领列表集中处理，大幅减少遗漏 |
| **状态管理** | Excel 单元格手工填 Y/N，颜色靠手动设置，多人协作时版本混乱 | 点击/右键一键切换状态，颜色自动标记，所有修改实时保存 |
| **进度交接** | 口头说明 + 看 Excel 批注，接手人需重新熟悉 | 项目文件一键加载，完整恢复全部状态，接手即用 |
| **归档整理** | 手工创建科目文件夹、复制文件、统一命名，耗时 1-2 小时 | 一键按科目/公司层级自动归档，耗时 10 秒 |
| **底稿导出** | 手工汇总核对结果，重新排版 | 一键导出三视图 Excel（核对总览 + 需求列表 + 文件浏览） |

**综合效率提升：一个中型审计项目的 PBC 核对环节，从平均 4-6 小时压缩到 1 小时以内。**

### 三、推广价值与可复制性

**推广价值：**

- **普适性强**：PBC 资料核对是审计行业的通用刚需，不限于特定行业或特定事务所。只要是执行审计程序的团队，都需要做资料核对。
- **门槛低**：工具已打包为单文件 exe，双击即用，无需安装 Python 环境或配置数据库，非技术背景的审计人员也能直接上手。
- **效果可感知**：秒级自动匹配 + 一键归档，使用前后对比极其明显，一线审计人员体验后自发传播意愿强。

**可复制性：**

- 内置的科目模板（[资料表.xlsx](资料表.xlsx)）可根据不同事务所/项目的审计程序灵活替换，工具代码无需改动。
- 匹配引擎基于通用的关键词提取 + 分层模糊匹配算法，不依赖特定命名规范，跨项目、跨客户均适用。
- 支持本地 Ollama 部署，即使在内网环境（无法访问外部 API）也能使用 AI 辅助匹配功能。
- 项目文件为标准化 JSON 格式，可被其他脚本或工具读取，便于与事务所现有审计系统对接。

---

## 功能详细介绍

## 📋 一、PBC 需求清单生成（步骤 0）

**解决的问题：** 每次审计都要手工整理 PBC 需求清单，科目多、资料项杂，容易遗漏。

| 功能 | 作用说明 |
|------|----------|
| **科目选择生成** | 内置审计资料表模板，涵盖货币资金、应收应付、固定资产等常见科目。勾选需要的科目，一键生成标准化的 PBC 需求清单 Excel |
| **多公司支持** | 支持同时核对多家被审计单位（如 CN02、CN03），清单中每位公司都有独立的获取状态列 |
| **下载清单** | 生成后可下载 Excel 文件，交给客户填写各公司是否已提供对应资料（Y / N / 不完整 / N/A） |
| **上传已填写清单** | 客户填写完毕后，上传 Excel 即可恢复工作进度，自动解析各公司的填写状态 |

---

## 📂 二、文件夹扫描（步骤 1）

**解决的问题：** 客户提交的资料通常散落在复杂嵌套的文件夹中，难以手工逐个核对。

| 功能 | 作用说明 |
|------|----------|
| **深度递归扫描** | 输入客户资料文件夹路径，自动遍历所有子目录，列出全部文件 |
| **增量变化检测** | 支持多次扫描同一文件夹，自动识别新增、删除的文件/文件夹，精确展示变化量 |
| **自动触发匹配** | 扫描完成后自动执行智能匹配，无需额外操作 |

---

## 🔍 三、智能匹配引擎（步骤 2）

**解决的问题：** PBC 清单中的"需求资料名称"和客户提交的"实际文件名"写法常常不同（缩写、中英文混用、括号注释等），手工逐条比对耗时且易错。

| 功能 | 作用说明 |
|------|----------|
| **关键词模糊匹配** | 自动提取文件名中的核心关键词（忽略序号、日期等噪声），与扫描到的文件进行智能比对 |
| **分层匹配策略** | 第一层：全量关键词精确匹配（含括号内区分信息如 CN02/CN03、合并/单体）；第二层：核心关键词回退匹配（括号内仅为英文翻译/补充说明时），兼顾精确度与召回率 |
| **文件 + 文件夹双匹配** | 同时匹配文件和文件夹，适配客户以文件夹形式提交整套资料的情况 |
| **公司自动归属** | 从文件路径和文件名中自动识别公司简称（如父目录名含 CN02、文件名含公司代码），将匹配结果自动归属到对应公司 |
| **增量合并模式** | 客户补充提交资料后重新扫描，已匹配的条目不会丢失，新文件会自动追加到已有匹配结果中 |

### 🤖 AI 辅助匹配（可选增强）

当规则匹配无法识别某些清单项时，可启用 LLM 大模型辅助匹配：

| 功能 | 作用说明 |
|------|----------|
| **多模型支持** | 支持通义千问（推荐）、OpenAI GPT-4o、DeepSeek、Kimi、MiniMax、智谱 GLM-4、Ollama 本地模型 |
| **分层 AI 匹配** | 先用低成本 fast 模型（如 qwen-flash）快速匹配，低置信度条目自动升级到 plus 模型复核，兼顾速度与准确率 |
| **语义理解** | LLM 能理解中英文对应、业务含义匹配（如"银行存款明细表" ↔ "Bank Deposit Detail.xlsx"），覆盖规则匹配无法处理的复杂情况 |

---

## 📊 四、核对工作台（核心操作区）

**解决的问题：** 传统审计用 Excel 逐行核对，需要频繁切换窗口，效率低下且容易出错。

### 三种视图模式

| 视图 | 作用说明 |
|------|----------|
| **矩阵视图** | 以"科目 × 公司"交叉表展示所有需求项，每格显示 Y / N / 不完整 / N/A 状态，类似底稿的核对矩阵，一目了然 |
| **列表视图** | 按"科目 → 所需PBC → 需求资料 → 公司"层级展开，每项列出匹配到的具体文件（带超链接可直接打开） |
| **文件浏览视图** | 以表格形式展示所有扫描到的文件，按文件夹层级（一级/二级/三级…文件夹）分列显示，每个文件标注"已匹配/未匹配"状态及关联的 PBC 需求 |

### 单元格状态操作

| 功能 | 作用说明 |
|------|----------|
| **一键循环切换** | 点击单元格在 Y（已获取）→ N（未获取）→ 不完整 → N/A 之间循环切换 |
| **右键快捷菜单** | 右键直接选择目标状态，无需多次点击 |
| **列过滤** | 每列支持下拉筛选，快速定位特定状态的条目 |
| **列宽拖拽调整** | 自由拖拽列宽适应内容长度 |

### 文件重命名

| 功能 | 作用说明 |
|------|----------|
| **右键重命名** | 在列表视图中右键点击文件名，弹出对话框输入新名称 |
| **全链路生效** | 重命名后的名称同步反映在界面显示、导出 Excel 和文件整理环节 |
| **随项目持久化** | 重命名映射会随项目一起保存，下次打开不会丢失 |

---

## 🔧 五、手动匹配与公司归属

**解决的问题：** 自动匹配不够完美时需要人工干预；客户提交的文件没有按公司分开放时，需要手动分配归属。

| 功能 | 作用说明 |
|------|----------|
| **待认领处理** | 当一个 PBC 需求匹配到了文件但工具无法自动判断文件属于哪家公司时（比如文件名和路径中都没有公司标识），该条目进入"待认领"列表。在弹窗中为每项选择所属公司后一键确认 |
| **手动添加匹配** | 在文件浏览视图中，可将尚未匹配的文件手动关联到指定 PBC 需求项 |
| **取消匹配** | 误匹配时可以移除文件关联，恢复为"未匹配"状态，同时清理对应的公司归属记录 |
| **批量矩阵分配** | 支持将同一文件同时分配给多家公司的同一个 PBC 需求项（适用于多公司共用同一份资料的情况） |

---

## ✏️ 六、清单编辑

**解决的问题：** 审计过程中需要动态调整 PBC 需求清单（增加、修改、删除条目）。

| 功能 | 作用说明 |
|------|----------|
| **新增行** | 在清单任意位置（上方/下方/末尾）插入新的 PBC 需求项，填写科目、所需PBC、需求资料 |
| **内联编辑** | 双击单元格直接修改科目、所需PBC、需求资料名称，回车确认 |
| **删除行** | 右键菜单选择删除，从清单中移除该条目 |
| **行管理模式** | 一键切换进入编辑模式，解锁所有编辑操作 |

---

## 📁 七、文件整理

**解决的问题：** 核对完成后，需要将客户提交的散乱文件按科目归档到底稿目录中。

| 功能 | 作用说明 |
|------|----------|
| **自动归档** | 按"科目 / 需求资料"两级目录自动创建文件夹结构，将已匹配文件复制到对应位置 |
| **按公司分目录** | 当一个 PBC 项涉及多家公司时，自动在需求资料目录下按公司名称分子文件夹 |
| **自动命名** | 多公司共用文件自动加公司前缀（如 `CN02_科目余额表.xlsx`），避免同名覆盖；用户重命名的文件使用重命名后的名称 |
| **文件预览** | 支持在工具内直接预览 Excel 文件内容，无需切换到外部程序 |

---

## 💾 八、导出

**解决的问题：** 核对结果需要归档为底稿，供复核和存档。

| 功能 | 作用说明 |
|------|----------|
| **三视图导出** | 一份 Excel 包含三个 Sheet：**核对总览**（矩阵状态表，带颜色标记）、**需求列表**（明细 + 文件超链接，可点击打开本地文件）、**文件浏览**（按目录层级 + 匹配状态 + 关联需求） |
| **颜色标记** | 导出保留颜色：绿色 = 已获取、红色 = 未获取、黄色 = 不完整、灰色 = N/A |
| **使用重命名** | 导出时文件名使用用户自定义的重命名，而非原始文件名 |

---

## 🗂️ 九、项目管理

**解决的问题：** 审计项目通常持续数周，今天没核对完明天继续；同一时期可能有多个审计项目并行。

| 功能 | 作用说明 |
|------|----------|
| **项目保存** | 将当前所有状态（清单、扫描结果、匹配结果、单元格状态、重命名、编辑修改）完整保存为项目文件 |
| **项目加载** | 随时恢复之前保存的工作进度，精确恢复到离开时的状态 |
| **自动保存** | 修改后 3 秒自动防抖保存，防止意外关闭丢失进度 |
| **多项目切换** | 支持创建多个项目（如"2024年度审计-甲公司""2024年度审计-乙公司"），随时切换 |
| **项目删除** | 清理已完成或废弃的项目 |

---

## 🔒 十、会话隔离与数据持久化

**解决的问题：** 打开多个浏览器标签页同时处理不同项目时数据不会串；电脑重启不丢数据。

| 功能 | 作用说明 |
|------|----------|
| **标签页隔离** | 每个浏览器标签页拥有独立会话 ID，互不干扰 |
| **LRU 内存缓存** | 内存中保留最近 20 个活跃会话，冷会话自动写入磁盘释放内存 |
| **磁盘持久化** | 所有会话数据自动保存到磁盘 JSON 文件，重启电脑/重启工具后数据仍在 |
| **原子写入** | 先写临时文件再替换，防止进程崩溃导致数据损坏 |

---

## 🚀 十一、部署与使用

| 特性 | 说明 |
|------|------|
| **一键启动** | 双击 exe 自动启动服务并打开浏览器 |
| **PyInstaller 打包** | 无需安装 Python 环境，单文件即可运行 |
| **本地运行** | 所有数据存储在本地，无需联网（AI 匹配功能除外） |

---

## 📌 典型工作流程

```
1. 生成清单  →  选择科目 → 填入公司名称 → 下载 Excel 发给客户填写
2. 客户返还  →  上传已填写清单 → 自动恢复各公司填写状态
3. 扫描资料  →  输入客户提交的资料文件夹路径 → 自动扫描 + 匹配
4. 核对审查  →  矩阵视图中逐项确认 → 手动调整匹配 → 切换单元格状态 → 右键重命名文件
5. AI 兜底   →  启用 LLM 匹配未识别项（可选）
6. 待认领处理 →  为无法自动归属公司的文件手动指定所属公司
7. 整理归档  →  一键按科目/公司层级整理文件到底稿目录
8. 导出报告  →  导出三视图 Excel 作为底稿附件
```
