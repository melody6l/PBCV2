# 开发运行日志面板 — 实现方案

> **实现状态（2026-07-22）**：已按诊断事件模型落地。本文后续的早期代码示例保留作需求背景，实际实现以本节和代码为准。
>
> - 日志只保存在前端 `devLogs` 内存中，刷新即清空，最多 2,000 条。
> - L1 由 `matcher.explain_fuzzy_match()` 返回真实采用的 `exact` / `core_fallback` 策略和实际关键词。
> - 后端在决策发生处生成统一事件，不再根据最终结果事后推断。
> - L1 记录所有成功关联、未使用文件及未匹配清单项，不只取第一个文件。
> - L2/L3 只有成功解析到唯一路径并更新匹配结果后才记成功；同名文件记为 `path_ambiguous`，不会静默选第一个。
> - 文件位置使用相对扫描根目录的路径；日志不包含文件正文、API Key 或模型提示词。
> - 面板使用 DOM `textContent` 安全渲染，支持搜索、阶段/状态筛选、展开详情、Esc 关闭和焦点恢复。
> - 采用“文件表格 + 右侧详情”：每个真实文件一行展示 L1/L2/提取/L3/最终归属，文件夹不计入日志数量；点击或使用上下方向键切换文件。
> - 步骤 2 不再单独展示日志列表，只保留进度条上方的实时状态文字，完整诊断统一放在右上角日志面板。
> - 文件浏览视图采用文件维度统计：文件总数、已归属、建议确认、未归属；“建议确认”可打开人工确认面板进行预览、改选归属或保持未归属。
> - LLM 供应商支持 Dify Chat App：调用 `/chat-messages` blocking 接口，以 Bearer API Key 鉴权，从响应 `answer` 中解析结构化结果。
> - L3 原有实时提取进度继续保留；完成后转换为统一的 `extract` 与 `l3` 诊断事件。
> - L3 仍只处理 L1/L2 后未匹配的文件；PDF 会先提取文字层，文字不足时使用传入的 OCR 配置自动回退识别。
> - L3 以未归属文件为主体，对照全部清单项分类；置信度 ≥0.75 自动追加，0.45–0.75 保留建议，更低或清单项为 null 时保持未归属。

统一事件的主要字段为：`stage`、`event`、`status`、`file_name`、`relative_path`、`checklist_index`、`checklist_name`、`strategy`、`confidence`、`duration_ms`、`detail`。

## 概述

在当前项目右侧 Header 添加一个按钮，点击弹出日志面板，展示每一轮匹配中每个文件的完整识别路径和命中详情。日志只在本次处理生命周期内有效（刷新即清空），方便开发阶段快速定位问题。

---

## 一、前端改动

### 1.1 HTML：在 Header 添加按钮 + 日志面板浮层

**文件**: `templates/index.html`

#### (a) Header 按钮（约 line 32，`</div>` 之前）

在 `project-bar` 容器内部末尾、右侧加入按钮：

```html
<button class="btn btn-sm btn-outline-light dev-log-btn" id="dev-log-btn" title="运行日志">&#9776;</button>
```

位置说明：Header 中 `topbar-inner` 使用 `justify-content: space-between`，`brand` 居左、`project-bar` 居右。按钮加在 `project-bar` 内部末尾即可自然靠右。

#### (b) 日志面板浮层（在 `</body>` 之前）

```html
<!-- 运行日志弹窗 -->
<div class="dev-log-overlay hidden" id="dev-log-overlay">
  <div class="dev-log-panel">
    <div class="dev-log-header">
      <span class="dev-log-title">&#9776; 运行日志</span>
      <button class="btn btn-sm dev-log-close" id="dev-log-close">&times;</button>
    </div>
    <div class="dev-log-body" id="dev-log-body">
      <div class="dev-log-empty">暂无日志，执行匹配后将在此显示</div>
    </div>
  </div>
</div>
```

### 1.2 CSS：面板样式

**文件**: `static/css/style.css`

在文件末尾追加以下样式：

```css
/* ====== 开发运行日志 ====== */
.dev-log-btn {
  margin-left: 12px;
  font-size: 16px;
  line-height: 1;
  padding: 4px 8px;
  cursor: pointer;
  opacity: 0.7;
  transition: opacity 0.2s;
}
.dev-log-btn:hover { opacity: 1; }

.dev-log-overlay {
  position: fixed;
  top: 0; left: 0; right: 0; bottom: 0;
  background: rgba(0,0,0,0.3);
  z-index: 9999;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding-top: 80px;
}
.dev-log-panel {
  background: #fff;
  border-radius: 8px;
  box-shadow: 0 8px 32px rgba(0,0,0,0.25);
  width: 90%;
  max-width: 960px;
  max-height: 70vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}
.dev-log-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  background: var(--primary, #1a1a2e);
  color: #fff;
  flex-shrink: 0;
}
.dev-log-title { font-weight: 600; font-size: 15px; }
.dev-log-close {
  background: none; border: none; color: #fff;
  font-size: 20px; cursor: pointer; opacity: 0.7;
  line-height: 1; padding: 0 4px;
}
.dev-log-close:hover { opacity: 1; }
.dev-log-body {
  flex: 1;
  overflow-y: auto;
  padding: 12px 16px;
  font-family: "Cascadia Code", "Fira Code", "Consolas", monospace;
  font-size: 13px;
  line-height: 1.6;
}
.dev-log-empty {
  color: #999;
  text-align: center;
  padding: 40px 0;
  font-family: inherit;
}

/* 日志条目行 */
.dev-log-line {
  display: flex;
  align-items: baseline;
  gap: 6px;
  padding: 3px 0;
  border-bottom: 1px solid #f0f0f0;
  white-space: nowrap;
}
.dev-log-line:last-child { border-bottom: none; }

.dev-log-icon {
  flex-shrink: 0;
  width: 20px;
  text-align: center;
}
.dev-log-file {
  flex-shrink: 0;
  max-width: 220px;
  overflow: hidden;
  text-overflow: ellipsis;
  color: var(--primary, #1a1a2e);
  font-weight: 600;
}
.dev-log-source {
  flex-shrink: 0;
  padding: 0 6px;
  border-radius: 3px;
  font-size: 11px;
  font-weight: 600;
}
.dev-log-source.keyword { background: #e8f5e9; color: #2e7d32; }
.dev-log-source.llm { background: #fff3e0; color: #e65100; }
.dev-log-source.content { background: #e3f2fd; color: #1565c0; }
.dev-log-source.error { background: #fce4ec; color: #c62828; }

.dev-log-detail {
  flex: 1;
  overflow: hidden;
  text-overflow: ellipsis;
  color: #555;
}
.dev-log-item {
  flex-shrink: 0;
  color: #888;
  font-size: 12px;
}
.dev-log-time {
  flex-shrink: 0;
  color: #bbb;
  font-size: 11px;
}
.dev-log-status {
  flex-shrink: 0;
  font-size: 13px;
}
```

### 1.3 JavaScript：日志收集与面板交互

**文件**: `static/js/main.js`

#### (a) 新增全局变量（约 line 55，与 matchResults 等放一起）

```javascript
let devLogs = [];              // 运行日志数组
```

#### (b) 新增辅助函数 `addDevLog()`（在全局变量之后）

```javascript
// ====== 运行日志 ======
function addDevLog(file, source, detail, matchedItem, timeCost, status) {
  devLogs.push({
    file,
    source,        // "keyword" | "llm" | "content" | "error"
    detail,        // 命中详情，如 `企业名称:ABC公司, 统一代码:914...`
    matchedItem,   // 关联清单项名称，如 "CN02-企业法人营业执照"
    timeCost,      // 耗时（秒）
    status,        // "success" | "warning" | "error"
    time: new Date().toLocaleTimeString(),
  });
}

function renderDevLogs() {
  const body = document.getElementById("dev-log-body");
  if (!devLogs.length) {
    body.innerHTML = '<div class="dev-log-empty">暂无日志，执行匹配后将在此显示</div>';
    return;
  }
  body.innerHTML = devLogs.map(log => {
    const iconMap = { success: "✅", warning: "⚠️", error: "❌" };
    const sourceLabels = { keyword: "关键词匹配", llm: "LLM匹配", content: "内容识别", "ocr+llm": "OCR+LLM" };
    const sourceClass = log.source === "error" ? "error" : log.source;
    const detailText = log.detail || "-";
    const itemText = log.matchedItem ? `→ ${log.matchedItem}` : "→ 未匹配";
    const timeText = log.timeCost != null ? `${log.timeCost.toFixed(1)}s` : "";
    return `<div class="dev-log-line">
      <span class="dev-log-icon">${iconMap[log.status] || "ℹ️"}</span>
      <span class="dev-log-status">${iconMap[log.status] || "ℹ️"}</span>
      <span class="dev-log-file" title="${escapeHtml(log.file)}">${escapeHtml(log.file)}</span>
      <span class="dev-log-source ${sourceClass}">${sourceLabels[log.source] || log.source}</span>
      <span class="dev-log-detail" title="${escapeHtml(detailText)}">${escapeHtml(detailText)}</span>
      <span class="dev-log-item">${itemText}</span>
      <span class="dev-log-time">${timeText}</span>
    </div>`;
  }).join("");
}

function escapeHtml(text) {
  const div = document.createElement("div");
  div.textContent = text;
  return div.innerHTML;
}
```

#### (c) 在 `DOMContentLoaded` 中初始化按钮和面板事件（约 line 92，现有的 init 函数调用后）

追加两行：
```javascript
initDevLogPanel();
```

新增函数：
```javascript
function initDevLogPanel() {
  const btn = document.getElementById("dev-log-btn");
  const overlay = document.getElementById("dev-log-overlay");
  const closeBtn = document.getElementById("dev-log-close");
  if (!btn || !overlay) return;

  btn.addEventListener("click", () => {
    renderDevLogs();
    overlay.classList.remove("hidden");
  });
  closeBtn.addEventListener("click", () => {
    overlay.classList.add("hidden");
  });
  overlay.addEventListener("click", (e) => {
    if (e.target === overlay) overlay.classList.add("hidden");
  });
}
```

---

## 二、后端改动

核心思路：三个匹配端点 (`/api/match`, `/api/llm-match`, `/api/content-match`) 各自在响应中新增 `dev_logs` 数组，包含本次处理中每个文件的一条记录。前端在 doMatch() 完成后将所有阶段的 logs 汇总。

### 2.1 `matcher.py` — 无改动

L1 匹配的信息只需要文件名和清单项即可推断，不需要修改 matcher.py。关键信息（哪些关键词命中了）可以从 `extract_keywords()` 获取，由 app.py 收集。

### 2.2 `app.py` — 三个端点增加 `dev_logs` 输出

#### (a) `/api/match`（约 line 245）

修改 return jsonify，追加 `dev_logs` 字段。

关键数据结构：`results` 中每个已匹配项包含 `checklist_name`, `matched_files`, `matched_names`。需要为每个 `matched_files[i]` 生成一条日志。
当前无法获取具体的关键词值，因为 `match_files()` 没有返回。可以合理推断：只要 `match_count > 0`，就是关键词匹配命中。

在 `result_counts(results)` 之后，添加：

```python
dev_logs = []
for r in results:
    if r.get("status") == "已获取" and r.get("matched_files"):
        # 取第一个匹配文件作为代表
        file_path = r["matched_files"][0]
        file_name = os.path.basename(file_path)
        # 提取关键词
        core_kws, opt_kws = extract_keywords(r["checklist_name"])
        all_kws = core_kws + opt_kws
        detail = "匹配关键字: " + ", ".join(all_kws[:5])
        dev_logs.append({
            "file": file_name,
            "source": "keyword",
            "detail": detail,
            "matched_item": r["checklist_name"],
            "time_cost": 0,  # L1 是即时的，无法精确计时
            "status": "success",
        })
    elif r.get("status") == "未匹配":
        for f in (r.get("matched_files") or []):
            dev_logs.append({
                "file": os.path.basename(f),
                "source": "error",
                "detail": "自动匹配失败",
                "matched_item": "",
                "time_cost": 0,
                "status": "error",
            })
```

注意：需要在 `app.py` 顶部导入 `extract_keywords`：
```python
from matcher import match_files, result_counts, extract_keywords
```

#### (b) `/api/llm-match`（约 line 1010）

在 `updated_count` 计算完成后，添加 LLM 匹配的日志。需要为每个通过 LLM 匹配到的文件生成一条记录。

```python
# LLM 匹配日志
dev_logs = []
llm_matched_detail = []
for r in s["match_results"]:
    if r["index"] in llm_map:
        llm_item = llm_map[r["index"]]
        matched_name = llm_item.get("matched_name", "")
        confidence = llm_item.get("confidence", 0)
        source_tier = llm_item.get("source", "flash")
        detail = f"LLM匹配({source_tier}), 置信度{confidence:.2f}"
        dev_logs.append({
            "file": matched_name,
            "source": "llm",
            "detail": detail,
            "matched_item": r["checklist_name"],
            "time_cost": 0,  # 粗略计时可在前端加
            "status": "success",
        })
```

在 return jsonify 中加入：
```python
"dev_logs": dev_logs,
```

#### (c) `/api/content-match`（约 line 1181）

内容识别涉及 OCR 和 LLM 两步，需要从 `extraction_results` 中获取每个文件的 content_type 来判断是 OCR 还是纯文本提取。

在 `updated_count` 计算完成后，添加：

```python
# 内容匹配日志
dev_logs = []
for p in content_paths:
    basename = os.path.basename(p)
    result = extraction_results.get(p, {})
    ct = result.get("content_type", "")
    err = result.get("error")
    if err:
        dev_logs.append({
            "file": basename,
            "source": "error",
            "detail": f"提取失败: {err[:60]}",
            "matched_item": "",
            "time_cost": 0,
            "status": "error",
        })
        continue
    # 判断是否是 OCR
    is_ocr = ct == "ocr"
    source = "ocr+llm" if is_ocr else "content"
    # 查找这个文件是否被任何清单项匹配了
    matched_items = []
    for r in content_result.get("results", []):
        if r.get("matched_name") == basename and r.get("confidence", 0) >= 0.5:
            matched_items.append(r.get("index"))
    # 通过 index 反查 checklist_name
    checklist_name = ""
    for r in s["match_results"]:
        if r["index"] in matched_items:
            checklist_name = r["checklist_name"]
    if checklist_name:
        dev_logs.append({
            "file": basename,
            "source": source,
            "detail": f"内容识别{' (OCR)' if is_ocr else ''}, 匹配成功",
            "matched_item": checklist_name,
            "time_cost": 0,
            "status": "success",
        })
    else:
        dev_logs.append({
            "file": basename,
            "source": source,
            "detail": f"内容已提取{' (OCR)' if is_ocr else ''}, 未匹配到清单项",
            "matched_item": "",
            "time_cost": 0,
            "status": "warning",
        })
```

在 return jsonify 中加入：
```python
"dev_logs": dev_logs,
```

### 2.3 `app.py` 顶部导入追加

```python
from matcher import match_files, result_counts, extract_keywords
```

---

## 三、前端数据流：doMatch 中的日志串联

**文件**: `static/js/main.js` — `doMatch()` 函数（约 line 1512）

当前 doMatch() 的逻辑是：
1. 调用 `/api/match` → 得到 L1 结果
2. 如果勾选了 LLM → 调用 `/api/llm-match` → 得到 L2 结果
3. 如果勾选了内容识别 → 调用 `/api/content-match` → 得到 L3 结果

改动：在每个步骤完成后，将 `data.dev_logs` 合并到全局 `devLogs` 数组。

### 修改 `doMatch()`

在 `data.results` 赋值后追加日志收集：

```javascript
// L1 匹配完成后
devLogs = [];  // 重置
if (data.dev_logs) devLogs.push(...data.dev_logs);
```

在 `await runLlmMatch()` 之前/之后收集 L2 日志。需要修改 `runLlmMatch()` 返回值包含 `data`。

```javascript
// 改造 runLlmMatch，使其返回 data
async function runLlmMatch() {
  // ... 现有代码 ...
  const data = await r.json();
  // ... 现有处理 ...
  if (data.dev_logs) devLogs.push(...data.dev_logs);
  return data;
}
```

类似改造 `runContentMatch()`：

```javascript
async function runContentMatch() {
  // ... 现有代码 ...
  const data = await r.json();
  // ... 现有处理 ...
  if (data.dev_logs) devLogs.push(...data.dev_logs);
  return data;
}
```

这样每次 doMatch() 完成时，`devLogs` 就包含了 L1+L2+L3 所有文件的完整记录。

---

## 四、时间成本记录

后端精确计时比较重。建议前端做简单计时：

在 `doMatch()` 开始时记录 `const t0 = performance.now()`，然后在每个阶段完成后记录时间差，传递给 `addDevLog()`。

更简单的方案：后端在生成 `dev_logs` 时，记录当前时间戳，由前端计算差值。

--- 

## 五、完整文件修改清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `templates/index.html` | 追加 | Header 按钮 (line 32附近) + 日志面板弹窗 (`</body>`前) |
| `static/css/style.css` | 追加 | 全部 `.dev-log-*` 样式 (文件末尾) |
| `static/js/main.js` | 多处 | ① 全局变量 `devLogs` ② 新增 `addDevLog/ renderDevLogs/ initDevLogPanel` ③ 修改 `doMatch/ runLlmMatch/ runContentMatch` 接收 dev_logs |
| `app.py` | 修改 | ① 顶部导入 `extract_keywords` ② 三个端点各自返回 `dev_logs` |

---

## 六、预览效果

匹配完成后点击 Header 右侧按钮：

```
┌──────────────────────────────────────────────────────────────┐
│  ☰ 运行日志                                             ✕   │
├──────────────────────────────────────────────────────────────┤
│ ✅ 营业执照.pdf    关键词匹配  企业名称:ABC公司, 统一代码   → CN02  0.3s│
│ ✅ 身份证.jpg      内容识别  姓名:张三                    → CN05  2.1s│
│ ⚠️ 发票.png        OCR+LLM   公司名:未提取(置信度0.4)     → CN03  1.2s│
│ ❌ 旧章程.pdf      提取失败   百度OCR返回空                → 未匹配      │
│ ✅ 纳税申报.xlsx   关键词匹配  匹配关键字: 纳税, 申报       → CN08  0.1s│
│ ✅ 委托书.docx     LLM匹配    LLM匹配(flash), 置信度0.85  → CN07  0.6s│
└──────────────────────────────────────────────────────────────┘
```
