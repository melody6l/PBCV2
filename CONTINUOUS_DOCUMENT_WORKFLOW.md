# 持续资料核对与整理

项目现在会为扫描文件建立稳定资料 ID，并记录文件版本和多个位置。已有项目仍可继续
打开；首次重新扫描时会自动建立资料注册表。

## 连续扫描

- 同一路径内容变化：保留原需求匹配，记录新版本，不自动调用 OCR 或 AI。
- 改名或移动：通过内容指纹迁移原匹配。
- 相同内容出现在另一位置：记录为同一资料的多个位置，不重复匹配。
- 多公司项目：位于不同公司目录的文件不会因为内容相同而被合并。
- 整理目录：整理完成后自动登记为主资料库。以后扫描任一已登记本地来源时，其他
  可用本地来源也会一并扫描。
- 直接放入整理后的需求目录：自动继承该目录对应的需求，并标记为待确认。

资料浏览中的“内容已更新”可查看轻量结构变化。Excel目前显示新增/删除 Sheet、
使用区域及公式数量变化；这个检查完全在本机完成，不消耗模型 Token。

## 资料整理

单公司使用 `科目/需求/文件`，多公司使用 `科目/需求/公司/文件`。结构不会再根据
文件数量变化。

整理前会预检同名文件：

1. 默认保留两份，新文件使用 `_v2`、`_v3` 等名称；
2. 可将新文件放在原名称，旧文件改名保留为历史版本；
3. 可跳过冲突文件。

整理后路径成为首选位置，匹配结果与Excel导出链接随之更新。一个文件关联多个需求时，
只整理一份主文件，所有需求继续引用该主文件。

## 压缩包

- ZIP：内置支持。
- 7z：`py7zr` 随程序打包，用户无需安装解压软件。
- RAR：应用已集成 `rarfile` 接口；发布包还需随目标平台加入可再分发的 RAR 解压
  后端。后端缺失时会明确显示“组件不可用”，不会静默跳过。
- 加密压缩包：扫描时询问密码，密码只用于本次扫描，不保存。

安全限制包括路径穿越、最多 5000 个文件、2GB 解压总量和 512MB 单文件限制。

## 云端链接

SharePoint/OneDrive 文件会保存 Graph Item ID、eTag 和网页地址。Excel导出优先使用：

1. 整理后的本地首选路径；
2. SharePoint/OneDrive 网页地址；
3. 其他可用本地路径。

云端内容仍缓存到本地用于现有匹配和预览，但导出不会把缓存路径交给其他用户。
